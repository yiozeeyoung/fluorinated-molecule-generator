# 含氟分子生成器依赖包
# 化学计算和分子处理
rdkit==2024.03.5

# 数据处理
pandas>=1.5.0
numpy>=1.21.0

# 科学计算
scipy>=1.7.0

# 机器学习
scikit-learn>=1.0.0

# 数据可视化
matplotlib>=3.5.0
seaborn>=0.11.0

# 进度条
tqdm>=4.62.0

# 数据库操作
sqlite3  # 内置模块，无需安装

# 命令行参数解析
argparse  # 内置模块，无需安装

# 多进程
multiprocessing  # 内置模块，无需安装

# 其他标准库
logging  # 内置模块，无需安装
warnings  # 内置模块，无需安装
datetime  # 内置模块，无需安装
random  # 内置模块，无需安装
os  # 内置模块，无需安装
sys  # 内置模块，无需安装
json  # 内置模块，无需安装